export const userSelector = (state) => {
  return state.user;
};

export const premiumSelector = (state) => {
  return state.premium;
};

export const getAllState = (state) => {
  return state;
};

export const getCourseID = (state) => {
  return state.courseId;
};
